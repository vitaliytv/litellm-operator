apiVersion: auth.litellm.ai/v1alpha1
kind: Team
metadata:
  name: ai-team
spec:
  teamAlias: ai-team
  models:
    - gpt-4o
  membersWithRole:
    - userEmail: ai-team-admin@example.com
      role: admin
