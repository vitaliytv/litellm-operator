---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: litellminstances.litellm.litellm.ai
spec:
  group: litellm.litellm.ai
  names:
    kind: LiteLLMInstance
    listKind: LiteLLMInstanceList
    plural: litellminstances
    singular: litellminstance
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: The LiteLLM image being used
      jsonPath: .spec.image
      name: Image
      type: string
    - description: Redis secret reference
      jsonPath: .spec.redisSecretRef.nameRef
      name: Redis
      type: string
    - description: Whether ingress is enabled
      jsonPath: .spec.ingress.enabled
      name: Ingress
      type: string
    - description: Whether gateway is enabled
      jsonPath: .spec.gateway.enabled
      name: Gateway
      type: string
    - description: Secret creation status
      jsonPath: .status.secretCreated
      name: Secret
      type: string
    - description: Deployment creation status
      jsonPath: .status.deploymentCreated
      name: Deployment
      type: string
    - description: Service creation status
      jsonPath: .status.serviceCreated
      name: Service
      type: string
    - description: Ingress creation status
      jsonPath: .status.ingressCreated
      name: Ingress Created
      type: string
    - description: Age of the LiteLLM instance
      jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: LiteLLMInstance is the Schema for the litellminstances API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: LiteLLMInstanceSpec defines the desired state of LiteLLMInstance.
            properties:
              databaseSecretRef:
                properties:
                  keys:
                    properties:
                      dbnameSecret:
                        type: string
                      hostSecret:
                        type: string
                      passwordSecret:
                        type: string
                      usernameSecret:
                        type: string
                    required:
                    - dbnameSecret
                    - hostSecret
                    - passwordSecret
                    - usernameSecret
                    type: object
                  nameRef:
                    type: string
                required:
                - keys
                - nameRef
                type: object
              gateway:
                properties:
                  enabled:
                    type: boolean
                  host:
                    type: string
                required:
                - enabled
                - host
                type: object
              image:
                default: ghcr.io/berriai/litellm-database:main-v1.74.9.rc.1
                type: string
              ingress:
                properties:
                  enabled:
                    type: boolean
                  host:
                    type: string
                required:
                - enabled
                - host
                type: object
              masterKey:
                type: string
              models:
                items:
                  description: model instance used to create proxy server config map
                  properties:
                    additionalProperties:
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                    identifier:
                      type: string
                    liteLLMParams:
                      description: LiteLLMParams defines the LiteLLM parameters for
                        a model.
                      properties:
                        additionalProp1:
                          description: AdditionalProps contains additional properties
                          type: object
                          x-kubernetes-preserve-unknown-fields: true
                        api_base:
                          description: APIBase is the base URL for the API
                          type: string
                        api_key:
                          description: APIKey is the API key for the model
                          type: string
                        api_version:
                          description: APIVersion is the version of the API
                          type: string
                        auto_router_config:
                          description: AutoRouterConfig is the auto router config
                          type: string
                        auto_router_config_path:
                          description: AutoRouterConfigPath is the auto router config
                            path
                          type: string
                        auto_router_default_model:
                          description: AutoRouterDefaultModel is the auto router default
                            model
                          type: string
                        auto_router_embedding_model:
                          description: AutoRouterEmbeddingModel is the auto router
                            embedding model
                          type: string
                        aws_access_key_id:
                          description: AWSAccessKeyID is the AWS access key ID
                          type: string
                        aws_region_name:
                          description: AWSRegionName is the AWS region name
                          type: string
                        aws_secret_access_key:
                          description: AWSSecretAccessKey is the AWS secret access
                            key
                          type: string
                        budget_duration:
                          description: BudgetDuration is the budget duration
                          type: string
                        configurable_clientside_auth_params:
                          description: ConfigurableClientsideAuthParams are configurable
                            client-side auth parameters
                          items:
                            type: object
                            x-kubernetes-preserve-unknown-fields: true
                          type: array
                        custom_llm_provider:
                          description: CustomLLMProvider is the custom LLM provider
                          type: string
                        input_cost_per_pixel:
                          description: InputCostPerPixel is the cost per pixel for
                            input
                          type: string
                        input_cost_per_second:
                          description: InputCostPerSecond is the cost per second for
                            input
                          type: string
                        input_cost_per_token:
                          description: InputCostPerToken is the cost per input token
                          type: string
                        litellm_credential_name:
                          description: LiteLLMCredentialName is the LiteLLM credential
                            name
                          type: string
                        litellm_trace_id:
                          description: LiteLLMTraceID is the LiteLLM trace ID
                          type: string
                        max_budget:
                          description: MaxBudget is the maximum budget
                          type: string
                        max_file_size_mb:
                          description: MaxFileSizeMB is the maximum file size in MB
                          type: integer
                        max_retries:
                          description: MaxRetries is the maximum number of retries
                          type: integer
                        merge_reasoning_content_in_choices:
                          description: MergeReasoningContentInChoices indicates if
                            to merge reasoning content in choices
                          type: boolean
                        mock_response:
                          description: MockResponse is the mock response
                          type: string
                        model:
                          description: Model is the model name
                          type: string
                        model_info:
                          description: ModelInfo contains additional model information
                          type: object
                          x-kubernetes-preserve-unknown-fields: true
                        organization:
                          description: Organization is the organization name
                          type: string
                        output_cost_per_pixel:
                          description: OutputCostPerPixel is the cost per pixel for
                            output
                          type: string
                        output_cost_per_second:
                          description: OutputCostPerSecond is the cost per second
                            for output
                          type: string
                        output_cost_per_token:
                          description: OutputCostPerToken is the cost per output token
                          type: string
                        region_name:
                          description: RegionName is the region name for the service
                          type: string
                        rpm:
                          description: RPM is requests per minute
                          type: integer
                        stream_timeout:
                          description: StreamTimeout is the stream timeout in seconds
                          type: integer
                        timeout:
                          description: Timeout is the timeout in seconds
                          type: integer
                        tpm:
                          description: TPM is tokens per minute
                          type: integer
                        use_in_pass_through:
                          description: UseInPassThrough indicates if to use in pass
                            through
                          type: boolean
                        use_litellm_proxy:
                          description: UseLiteLLMProxy indicates if to use LiteLLM
                            proxy
                          type: boolean
                        vertex_credentials:
                          description: VertexCredentials is the credentials for Vertex
                            AI
                          type: string
                        vertex_location:
                          description: VertexLocation is the location for Vertex AI
                          type: string
                        vertex_project:
                          description: VertexProject is the Google Cloud project for
                            Vertex AI
                          type: string
                        watsonx_region_name:
                          description: WatsonXRegionName is the WatsonX region name
                          type: string
                      type: object
                    modelCredentials:
                      properties:
                        keys:
                          properties:
                            apiBase:
                              type: string
                            apiKey:
                              type: string
                            awsAccessKeyId:
                              type: string
                            awsSecretAccessKey:
                              type: string
                            vertexCredentials:
                              type: string
                            vertexProject:
                              type: string
                          type: object
                        nameRef:
                          type: string
                      required:
                      - keys
                      - nameRef
                      type: object
                    modelName:
                      type: string
                    requiresAuth:
                      type: boolean
                  required:
                  - identifier
                  - requiresAuth
                  type: object
                type: array
              redisSecretRef:
                properties:
                  keys:
                    properties:
                      hostSecret:
                        type: string
                      passwordSecret:
                        type: string
                      portSecret:
                        type: string
                    required:
                    - hostSecret
                    - passwordSecret
                    - portSecret
                    type: object
                  nameRef:
                    type: string
                required:
                - keys
                - nameRef
                type: object
              replicas:
                default: 1
                format: int32
                type: integer
            required:
            - image
            type: object
          status:
            description: LiteLLMInstanceStatus defines the observed state of LiteLLMInstance.
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of a LiteLLM instance's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              configMapCreated:
                description: Resource creation status
                type: boolean
              deploymentCreated:
                type: boolean
              ingressCreated:
                type: boolean
              lastUpdated:
                description: LastUpdated represents the last time the status was updated
                format: date-time
                type: string
              observedGeneration:
                description: ObservedGeneration represents the .metadata.generation
                  that the condition was set based upon
                format: int64
                type: integer
              secretCreated:
                type: boolean
              serviceCreated:
                type: boolean
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
