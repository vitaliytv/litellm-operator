---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: teams.auth.litellm.ai
spec:
  group: auth.litellm.ai
  names:
    kind: Team
    listKind: TeamList
    plural: teams
    singular: team
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Team is the Schema for the teams API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: TeamSpec defines the desired state of Team
            properties:
              blocked:
                description: Blocked is a flag indicating if the team is blocked or
                  not - will stop all calls from keys with this team_id
                type: boolean
              budgetDuration:
                description: BudgetDuration - Budget is reset at the end of specified
                  duration. If not set, budget is never reset. You can set duration
                  as seconds ("30s"), minutes ("30m"), hours ("30h"), days ("30d"),
                  months ("1mo").
                type: string
              guardrails:
                description: Guardrails are guardrails for the team
                items:
                  type: string
                type: array
              maxBudget:
                description: MaxBudget is the maximum budget for the team
                type: string
              metadata:
                additionalProperties:
                  type: string
                description: Metadata is the metadata of the team
                type: object
              modelAliases:
                additionalProperties:
                  type: string
                description: ModelAliases are model aliases for the team
                type: object
              models:
                description: Models is the list of models that are associated with
                  the team. All keys for this team_id will have at most, these models.
                  If empty, assumes all models are allowed.
                items:
                  type: string
                type: array
              organizationID:
                description: OrganizationID is the ID of the organization that the
                  team belongs to. If not set, the team will be created with no organization.
                type: string
              rpmLimit:
                description: RPMLimit is the maximum requests per minute limit for
                  the team - all keys associated with this team_id will have at max
                  this RPM limit
                type: integer
              tags:
                description: Tags for tracking spend and/or doing tag-based routing.
                  Requires Enterprise license
                items:
                  type: string
                type: array
              teamAlias:
                description: TeamAlias is the alias of the team
                type: string
                x-kubernetes-validations:
                - message: TeamAlias is immutable
                  rule: self == oldSelf
              teamID:
                description: TeamID is the ID of the team. If not set, a unique ID
                  will be generated.
                type: string
              teamMemberPermissions:
                description: 'TeamMemberPermissions is the list of routes that non-admin
                  team members can access. Example: ["/key/generate", "/key/update",
                  "/key/delete"]'
                items:
                  type: string
                type: array
              tpmLimit:
                description: TPMLimit is the maximum tokens per minute limit for the
                  team - all keys with this team_id will have at max this TPM limit
                type: integer
            required:
            - teamAlias
            type: object
          status:
            description: TeamStatus defines the observed state of Team
            properties:
              blocked:
                description: Blocked is a flag indicating if the team is blocked or
                  not
                type: boolean
              budgetDuration:
                description: BudgetDuration - Budget is reset at the end of specified
                  duration. If not set, budget is never reset.
                type: string
              budgetResetAt:
                description: BudgetResetAt is the date and time when the budget will
                  be reset
                type: string
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              createdAt:
                description: CreatedAt is the date and time when the team was created
                type: string
              liteLLMModelTable:
                description: LiteLLMModelTable is the model table for the team
                type: string
              maxBudget:
                description: MaxBudget is the maximum budget for the team
                type: string
              maxParallelRequests:
                description: MaxParallelRequests is the maximum number of parallel
                  requests allowed
                type: integer
              membersWithRole:
                description: MembersWithRole is the list of members with role
                items:
                  properties:
                    role:
                      description: Role is the role of the user - one of "admin" or
                        "user"
                      type: string
                    userEmail:
                      description: UserEmail is the email of the user
                      type: string
                    userID:
                      description: UserID is the ID of the user
                      type: string
                  type: object
                type: array
              modelID:
                description: ModelID is the ID of the model
                type: string
              models:
                description: Models is the list of models that are associated with
                  the team. All keys for this team_id will have at most, these models.
                items:
                  type: string
                type: array
              organizationID:
                description: OrganizationID is the ID of the organization that the
                  team belongs to
                type: string
              rpmLimit:
                description: RPMLimit is the maximum requests per minute limit for
                  the team - all keys associated with this team_id will have at max
                  this RPM limit
                type: integer
              spend:
                description: Spend is the current spend of the team
                type: string
              tags:
                description: Tags for tracking spend and/or doing tag-based routing.
                  Requires Enterprise license
                items:
                  type: string
                type: array
              teamAlias:
                description: TeamAlias is the alias of the team
                type: string
              teamID:
                description: TeamID is the ID of the team
                type: string
              teamMemberPermissions:
                description: TeamMemberPermissions is the list of routes that non-admin
                  team members can access
                items:
                  type: string
                type: array
              tpmLimit:
                description: TPMLimit is the maximum tokens per minute limit for the
                  team - all keys with this team_id will have at max this TPM limit
                type: integer
              updatedAt:
                description: UpdatedAt is the date and time when the team was last
                  updated
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
