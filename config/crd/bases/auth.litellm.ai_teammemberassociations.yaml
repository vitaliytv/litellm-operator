---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: teammemberassociations.auth.litellm.ai
spec:
  group: auth.litellm.ai
  names:
    kind: TeamMemberAssociation
    listKind: TeamMemberAssociationList
    plural: teammemberassociations
    singular: teammemberassociation
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: The ready status of the association
      jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - description: The team alias
      jsonPath: .spec.teamAlias
      name: Team
      type: string
    - description: The user's email address
      jsonPath: .spec.userEmail
      name: User
      type: string
    - description: The user's role in the team
      jsonPath: .spec.role
      name: Role
      type: string
    - description: Maximum budget for user in team
      jsonPath: .spec.maxBudgetInTeam
      name: Budget
      type: string
    - description: Time since creation
      jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: TeamMemberAssociation is the Schema for the teammemberassociations
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: TeamMemberAssociationSpec defines the desired state of TeamMemberAssociation
            properties:
              connectionRef:
                description: ConnectionRef defines how to connect to the LiteLLM instance
                properties:
                  instanceRef:
                    description: InstanceRef references a LiteLLM instance
                    properties:
                      name:
                        description: Name is the name of the LiteLLM instance
                        type: string
                      namespace:
                        description: Namespace is the namespace of the LiteLLM instance
                          (defaults to the same namespace as the Team)
                        type: string
                    required:
                    - name
                    type: object
                  secretRef:
                    description: SecretRef references a secret containing connection
                      details
                    properties:
                      keys:
                        description: Keys defines the keys in the secret that contain
                          connection details
                        properties:
                          masterKey:
                            description: MasterKey is the key in the secret containing
                              the master key
                            type: string
                          url:
                            description: URL is the key in the secret containing the
                              LiteLLM URL
                            type: string
                        required:
                        - masterKey
                        - url
                        type: object
                      name:
                        description: Name is the name of the secret
                        type: string
                    required:
                    - keys
                    - name
                    type: object
                type: object
              maxBudgetInTeam:
                description: MaxBudgetInTeam is the maximum budget for the user in
                  the team
                type: string
              role:
                description: Role is the role of the user - one of "admin" or "user"
                enum:
                - admin
                - user
                type: string
              teamAlias:
                description: TeamID is the ID of the team
                type: string
                x-kubernetes-validations:
                - message: TeamAlias is immutable
                  rule: self == oldSelf
              teamRef:
                description: TeamRef is a reference to the team
                properties:
                  name:
                    type: string
                  namespace:
                    type: string
                type: object
              userEmail:
                description: UserEmail is the email of the user
                type: string
                x-kubernetes-validations:
                - message: UserEmail is immutable
                  rule: self == oldSelf
              userRef:
                description: UserRef is a reference to the user
                properties:
                  name:
                    type: string
                  namespace:
                    type: string
                type: object
            required:
            - connectionRef
            - role
            - teamAlias
            - teamRef
            - userEmail
            - userRef
            type: object
          status:
            description: TeamMemberAssociationStatus defines the observed state of
              TeamMemberAssociation
            properties:
              associationIsValid:
                type: boolean
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              teamAlias:
                description: TeamAlias is the alias of the team
                type: string
              teamExists:
                type: boolean
              teamID:
                description: TeamID is the ID of the team
                type: string
              userEmail:
                description: UserEmail is the email of the user
                type: string
              userExists:
                type: boolean
              userID:
                description: UserID is the ID of the user
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
